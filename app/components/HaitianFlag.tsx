export default function HaitianFlag({ className }: { className?: string }) {
    // eslint-disable-next-line @next/next/no-img-element
    return <img src="/haiti-flag.svg" alt="Drapeau d'Haïti" className={className} />;
}
